UNIV-ASSIST React Project (Vite)

How to run:
1) cd univ_assist_react
2) npm install
3) npm run dev

The app starts at http://localhost:5173 by default

To build: npm run build
To preview build: npm run preview

Note: Connect API base URL in code if backend running at http://localhost:8000
